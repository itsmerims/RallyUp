# Repository Instructions

## Git workflow

- After every completed code change, create a Git commit containing the relevant changes and push that commit to the configured upstream branch. Verify the change before committing when practical. Do not include unrelated user changes in the commit.
